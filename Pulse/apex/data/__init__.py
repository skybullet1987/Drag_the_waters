"""Pulse.apex.data — custom data sources and PythonData adapters.

These modules pair with `Pulse.apex.signals.<name>` to provide:
  - The PythonData class (QC custom-data subscription)
  - The data-loading helper (CSV fixture / URL fetch)
  - The compute_<name>_score helper that produces a SignalScore
"""
