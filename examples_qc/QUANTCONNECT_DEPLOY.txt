================================================================================
Conditional Sector Rotation — QuantConnect deployment (~60x baseline)
================================================================================

RESTORED BASELINE (commit ae73726 logic, May 2026)
--------------------------------------------------------------------------------
The ~60x run (~$6M, ~900 orders) used this single-file algorithm BEFORE later
120x/LIFT experiments added _safe_set_holdings churn, VIX gates, and csr_profiles
splits that degraded results to ~$3.2M / ~1700 orders.

UPLOAD FOR ~60x BASELINE — ONE FILE ONLY
--------------------------------------------------------------------------------
  conditional_sector_rotation_improved.py  →  rename to  main.py

Do NOT upload csr_profiles.py for this baseline (main.py has no import).

File size ~50k — under QuantConnect 64k limit.

At top of main.py (already set):
  USE_QC_UI_PARAMETERS = False

No QuantConnect parameter panel settings required.

Expected log at start:
  BASELINE_60X_RESTORED: ae73726 logic, hardcoded 2020-2026, $100k, ...
  MAXIMIZE_BACKTEST_EQUITY profile: same-bar, rails off, hot vol targets, ...
  EXECUTION_MODE=same_bar (maximize baseline)

Expected ballpark (2020-01-01 .. 2026-05-17, $100k):
  End equity ~ $6M+   |  Orders ~900   |  CAGR ~90%   |  Max DD ~45%

If you still see ~$3M / ~1700 orders:
  - Old main.py still on QC (with csr_profiles import) — replace entirely
  - Remove csr_profiles.py from the QC project if present
  - Delete the algorithm and re-create with only the new main.py

OPTIONAL: csr_profiles.py path
--------------------------------------------------------------------------------
Only for future experiments. Not used by the restored baseline.

BENCHMARK (separate QC project)
--------------------------------------------------------------------------------
  benchmark_tqqq_buy_hold.py

================================================================================
