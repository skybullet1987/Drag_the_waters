================================================================================
Conditional Sector Rotation — QuantConnect deployment
================================================================================

FILE SIZE: every .py file must be under 63,000 bytes.

IB PAPER / LIVE (recommended for IBKR) — TWO FILES
--------------------------------------------------------------------------------
  See IB_PAPER_DEPLOY.txt

  main.py + csr_ml_overlay.py
  ACTIVE_BASELINE = "ib_paper"

  EOD next-bar + ML v2 defensive + rails/DD guard (NOT same-bar maximize).

RESEARCH BACKTESTS (2016+ or 2020-2026, $100k)
--------------------------------------------------------------------------------
  maximize                ~346x (10y)   ~75% CAGR   ~50% DD   (1 file) same-bar
  ml_overlay              ~170x (10y)   ~64% CAGR   ~48% DD   (2 files)
  ml_overlay_diversified  failed vs ml on 2020-26 (3 files) — experimental

DEPRECATED / FAILED
--------------------------------------------------------------------------------
  ml_overlay aggressive, maximize_hold, convex, institutional, maximize_plus

MAXIMIZE — ONE FILE
--------------------------------------------------------------------------------
  ACTIVE_BASELINE = "maximize"   (backtest only — not for live)

ML OVERLAY v2 — TWO FILES
--------------------------------------------------------------------------------
  ACTIVE_BASELINE = "ml_overlay"   (backtest; same-bar unless production_safe)

MAXIMIZE PAPER / LIVE — add third file (does NOT change backtests)
--------------------------------------------------------------------------------
  csr_live_margin_ext.py  (+ main.py; + csr_ml_overlay.py if ml_overlay)

  ROTATE_DEFER / ROTATE_BUY: LiveMode only. Backtest uses same-bar + MARGIN_CLAMP
  (no defer) to avoid insufficient buying power on large accounts without killing CAGR.

================================================================================
